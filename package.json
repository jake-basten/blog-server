{
    "name": "blog-server",
    "version": "0.0.1",
    "scripts": {
        "build": "sam build",
        "local": "npm run build && sam local start-api"
    },
    "dependencies": {
        "aws-sdk": "^2.799.0"
    }
}