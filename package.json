{
  "name": "blog-server",
  "version": "0.0.1",
  "scripts": {
    "compile": "tsc",
    "build": "sam build",
    "local": "npm run compile && npm run build && sam local start-api"
  },
  "devDependencies": {
    "aws-sdk": "^2.799.0",
    "@types/aws-lambda": "^8.10.72",
    "@types/node": "^14.14.34",
    "typescript": "^4.2.3"
  }
}
