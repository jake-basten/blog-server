{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "build": "sam build",
        "local": "npm run build && sam local start-api"
    },
    "dependencies": {
        "aws-sdk": "^2.799.0"
    }
}
